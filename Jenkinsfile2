pipeline{
    agent any
    tools {
        maven 'M2_HOME'
    }
    stages{
        stage('sonarqubescan'){
            steps{
                sh 'mvn soanr:sonar'
            }
        }
        stage('all maven commands1'){
            steps{
                sh 'mvn clean test compile install package'
            }
        }
    }
}