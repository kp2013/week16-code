pipeline{
    agent any
    tools {
        maven 'M2_HOME'
    }
    stages{
        stage('maven commands'){
            steps{
                sh 'mvn clean test1 compile install package'
            }
        }
    }
}